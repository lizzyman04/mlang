module utils

txt greet(txt name) {
    return "Hello, " + name;
}

int clamp(int val, int lo, int hi) {
    if val < lo {
        return lo;
    }
    if val > hi {
        return hi;
    }
    return val;
}
